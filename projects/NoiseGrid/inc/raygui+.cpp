#include "raygui+.h"
/*
void rBounds::add_child(rBounds *child) {
    children.push_back(child);
}

void rBounds::set_anchor(rBounds *parent, rBounds::Anchor anchor) {
    this->parent = parent;
    this->anchor = anchor;
}

void rBounds::update_to_parent() { // Anchor::None means no update to bounds.x and bounds.y
    if (anchor == ANCHOR_TOP_LEFT || anchor == ANCHOR_BOTTOM_LEFT) { bounds.x = parent->bounds.x; }
    if (anchor == ANCHOR_TOP_LEFT || anchor == ANCHOR_TOP_RIGHT) { bounds.y = parent->bounds.y; }
    if (anchor == ANCHOR_TOP_RIGHT || anchor == ANCHOR_BOTTOM_RIGHT) { bounds.x = parent->bounds.x + parent->bounds.width - bounds.width; }
    if (anchor == ANCHOR_BOTTOM_LEFT || anchor == ANCHOR_BOTTOM_RIGHT) { bounds.y = parent->bounds.y + parent->bounds.height - bounds.height; }
}

void rBounds::move(float x, float y) {
    bounds.x = x;
    bounds.y = y;
}

void rBounds::resize(float w, float h) {
    bounds.width = w;
    bounds.height = h;
}
*/

